import React from "react";
import { todoType } from "../../d";

const TodoTrue = (props: todoType) => {
  const { title, contents, isDone, id } = props;
  return <>{/* <h4>Done!</h4> */}</>;
};

export default TodoTrue;
